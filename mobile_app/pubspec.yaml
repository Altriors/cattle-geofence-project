name: cattle_geofence
description: Cattle Geo-Fence Boundary Monitoring App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.14.0
  firebase_messaging: ^14.7.10
  firebase_storage: ^11.6.0

  # Notifications
  flutter_local_notifications: ^16.3.0

  # State Management
  provider: ^6.1.1

  # Camera
  camera: ^0.10.5+9

  # Utils
  intl: ^0.19.0
  path: ^1.9.0
  path_provider: ^2.1.2

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
